/* Routes Covered — KEPT from the Operations Center. Coverage roll-up ports
   here in Phase 4, derived live from the Asset Matrix assignments. */
export default function CoveredView() {
  return (
    <div className="am-page">
      <div className="am-head"><h2>Routes Covered</h2></div>
      <div className="am-note">
        <p><b>Kept feature — full port lands in Phase 4.</b> The covered-routes roll-up will
          derive live from Asset Matrix assignments (no separate data entry), the same way
          Bravo's boards derive from loads.</p>
      </div>
    </div>
  );
}
