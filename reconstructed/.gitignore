# dependencies / build output
node_modules/
app/dist/

# local env (Firebase web config — recreate via `firebase apps:sdkconfig`, see README)
.env
.env.local
.env.*.local
app/.env.local
app/.env.development.local

# firebase cache
.firebase/
firebase-debug.log

# OS noise
Thumbs.db
.DS_Store
functions/lib/

# functions params are NOT secret (secrets live in Secret Manager) — keep in git
!functions/.env

# separate project parked in this folder - never commit
Master Dispatch - DB/
